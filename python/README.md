# Python Analysis Layer

This folder is intentionally isolated.
Python scripts:
- Read raw or exported JSON
- Perform analysis / ML
- Output artifacts to /artifacts

Python never talks to Firebase directly.
